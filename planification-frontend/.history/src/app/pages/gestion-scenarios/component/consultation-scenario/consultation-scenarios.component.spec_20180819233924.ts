import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { ConsulationScenarioComponent } from './consultation-scenarios.component';


describe('ConsultationScenarioComponent', () => {
  let component: ConsulationScenarioComponent;


describe('ConsultationScenarioComponent', () => {
  let component: ConsulationScenarioComponent;
  let fixture: ComponentFixture<ConsulationScenarioComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ConsulationScenarioComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ConsulationScenarioComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ConsulationScenarioComponent ]
    })
    .compileComponents();
  }));

 

 
});
